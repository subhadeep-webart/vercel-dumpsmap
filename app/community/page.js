// /community → permanent redirect to /activity-hub
// ---------------------------------------------------------------------------
// Activity Hub is now the single source of truth for the unified feed. The
// legacy Community top-level page has been retired (Groups/Guidelines sub-routes
// remain accessible at /community/groups and /community/guidelines). Detail
// pages /community/posts/:id are still served from this directory.

// import { redirect } from 'next/navigation'

// export default function CommunityIndexRedirect() {
//   redirect('/activity-hub')
// }
